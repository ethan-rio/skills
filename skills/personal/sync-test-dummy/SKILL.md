---
name: sync-test-dummy
description: Throwaway skill used to exercise local/sync.sh — tests the "new skill, copy into repo" branch. Safe to delete after testing.
---

# sync-test-dummy

No behavior. This skill exists only to verify that `local/sync.sh` detects a new
skill in `~/.claude/skills/` and offers to copy it into the repo.
